using System.Collections.Generic;
using UnityEngine;

public class AllyAI : MonoBehaviour
{
    // private Entity entity;
    // private List<GameAction> availableActions;

    // private void Start()
    // {
    //     entity = GetComponent<Entity>();
    //     availableActions = GetComponent<ActionContainer>().GetActions();
    // }

    // public ActionExecution DecideAction()
    // {
    //     // Simple priority-based decision making
    //     foreach (var action in availableActions)
    //     {
    //         if (action is HealAction && NeedsHealing())
    //         {
    //             return new ActionExecution(action, entity, FindHealTarget());
    //         }
    //         if (action is AttackAction)
    //         {
    //             return new ActionExecution(action, entity, FindAttackTarget());
    //         }
    //     }
    //     return null;
    // }
}
